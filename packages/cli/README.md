# AtomJS CLI

Run and build AtomJS applications.

```bash
atom run dev
atom run build
atom build windows
atom build macos
atom build linux
atom build all
```

Project: https://github.com/Atom-js-org/atom
